# CPU environment verified on Grid'5000 for repository utilities.
python==3.10.19
numpy==2.2.6
pandas==2.3.3
scipy==1.15.2
scikit-learn==1.7.2
shap==0.49.1
PyYAML==6.0.3
torch==2.5.1
tabicl==2.0.2

# XGBoost 3.2.0 passed a CUDA probe on musa-1 with V3 data. The historical
# production wheel could not be recovered, so this is a compatibility pin,
# not a claim about the original wheel.
xgboost==3.2.0
tabpfn==8.0.7
# TabICLv2 was executed through the tabicl==2.0.2 package above.
pysr==1.5.9
# SymbolicRegression.jl==1.11.3  # Julia 1.10 compatibility declared by Project.toml
# CUDA runtime observed on the GPU probe: 12.1.

# Model checkpoints and hashes belong in docs/runtime-manifest.yaml.
