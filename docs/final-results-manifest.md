# Final results manifest

The current final V3 tables are retained outside this repository as the
versioned artifact `403_final_tables_100_replicas_20260816`.

The repository publishes a redacted manifest and checksums, not private raw
data or the complete historical monitoring tree. Before the first GitHub
release, replace the artifact name with its approved public DOI, repository
release, or access procedure and record the release checksum.
